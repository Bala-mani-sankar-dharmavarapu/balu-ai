import { Box } from "@mui/material";

const KaleReview = () => {
  return (
    <Box sx={{ p: 3, textAlign: "center", fontSize: "1.5rem" }}>
      Kale Review
    </Box>
  );
};

export default KaleReview;
